<%= form_with(model: hook, local: true) do |form| %>
  <% if hook.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(hook.errors.count, "error") %> prohibited this hook from being saved:</h2>

      <ul>
      <% hook.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :payload %>
    <%= form.text_field :payload %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
